# Dev-grade PostgreSQL for the migrated service. M3 copies this file to
# k8s/postgres.yaml only when evidence/required-extensions.json
# database.needed is true. deploy-app applies k8s/ only — this directory
# is not applied at scaffold time.
apiVersion: v1
kind: Secret
metadata:
  name: iso-worker-c-postgres
  labels:
    app.kubernetes.io/part-of: iso-worker-c
type: Opaque
stringData:
  # DEMO VALUES ONLY — dest-local postgres, not a production secret.
  POSTGRESQL_USER: appuser
  POSTGRESQL_PASSWORD: apppass
  POSTGRESQL_DATABASE: iso-worker-c
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iso-worker-c-postgres
  labels:
    app.kubernetes.io/part-of: iso-worker-c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iso-worker-c-postgres
  template:
    metadata:
      labels:
        app: iso-worker-c-postgres
    spec:
      containers:
        - name: postgresql
          image: registry.redhat.io/rhel9/postgresql-15:latest
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: iso-worker-c-postgres
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "appuser", "-d", "iso-worker-c"]
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: {cpu: 100m, memory: 256Mi}
            limits: {memory: 512Mi}
          volumeMounts:
            - name: data
              mountPath: /var/lib/pgsql/data
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: iso-worker-c-postgres
  labels:
    app.kubernetes.io/part-of: iso-worker-c
spec:
  selector:
    app: iso-worker-c-postgres
  ports:
    - port: 5432
      targetPort: 5432
